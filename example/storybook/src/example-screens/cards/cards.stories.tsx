import type { ComponentMeta } from '@storybook/react-native';
import Cards from './cards';
const CardsMeta: ComponentMeta<typeof Cards> = {
  title: 'example-screens/Cards',
  component: Cards,
};

export default CardsMeta;
export { Cards };
