import type { ComponentMeta } from '@storybook/react-native';
import Forms from './forms';
const FormsMeta: ComponentMeta<typeof Forms> = {
  title: 'example-screens/Forms',
  component: Forms,
};

export default FormsMeta;
export { Forms };
