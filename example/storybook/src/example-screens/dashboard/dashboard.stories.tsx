import type { ComponentMeta } from '@storybook/react-native';
import Dashboard from './dashboard';
const DashboardMeta: ComponentMeta<typeof Dashboard> = {
  title: 'example-screens/Dashboard',
  component: Dashboard,
};

export default DashboardMeta;
export { Dashboard };
