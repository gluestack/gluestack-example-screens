import type { ComponentMeta } from '@storybook/react-native';
import music from './music';
const musicMeta: ComponentMeta<typeof music> = {
  title: 'example-screens/music',
  component: music,
};

export default musicMeta;
export { music };
