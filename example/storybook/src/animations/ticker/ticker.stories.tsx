import type { ComponentMeta } from '@storybook/react-native';
import Ticker from './ticker';
const TickerMeta: ComponentMeta<typeof Ticker> = {
  title: 'animations/Ticker',
  component: Ticker,
};

export default TickerMeta;
export { Ticker };
