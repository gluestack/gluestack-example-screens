import type { ComponentMeta } from '@storybook/react-native';
import FlipCard from './flip-card';
const FlipCardMeta: ComponentMeta<typeof FlipCard> = {
  title: 'animations/FlipCard',
  component: FlipCard,
};

export default FlipCardMeta;
export { FlipCard };
