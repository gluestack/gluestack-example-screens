import type { ComponentMeta } from '@storybook/react-native';
import Login from './login';
const LoginMeta: ComponentMeta<typeof Login> = {
  title: 'nativewind-example-screens/Login',
  component: Login,
};

export default LoginMeta;
export { Login };
